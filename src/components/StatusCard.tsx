export function StatusCard({label,value,detail,status}:{label:string,value:string,detail:string,status?:string}){return <div className="card"><div className="muted" style={{fontSize:12}}>{label}</div><div className="kpi">{value}</div><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginTop:12}}><span className="muted" style={{fontSize:12}}>{detail}</span>{status&&<span className="pill"><span className="dot"/>{status}</span>}</div></div>}
