'use client';
import Link from 'next/link';
import {useSearchParams} from 'next/navigation';
import {useState} from 'react';
import {CheckCircle2,KeyRound} from 'lucide-react';
export default function ResetPassword(){const params=useSearchParams();const token=params.get('token')||'';const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [busy,setBusy]=useState(false);const [done,setDone]=useState(false);const [error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(password.length<8){setError('Use at least 8 characters.');return}if(password!==confirm){setError('Passwords do not match.');return}setBusy(true);const r=await fetch('/api/auth/password-reset/confirm',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,password})});const d=await r.json().catch(()=>({}));if(r.ok)setDone(true);else setError(d.error||'Unable to reset password.');setBusy(false)};return <main className="loginPage"><section className="loginLayout"><div className="loginStory"><Link className="loginBrand" href="/"><span>Daiki<span>●</span></span><small>AI Passport</small></Link><div className="storyCopy"><div className="eyebrow">New password</div><h1>A fresh key<br/>for your space.</h1><p>Choose a new password for your Daiki account. Reset links are single-use and expire after 15 minutes.</p></div><div className="storyFooter"><KeyRound size={16}/> Your active Daiki sessions are signed out after a successful reset.</div></div><div className="loginPanel">{done?<><CheckCircle2 size={38}/><div><div className="eyebrow">All set</div><h2>Password updated</h2><p className="muted">Your new password is ready. Sign in again to continue.</p></div><Link href="/login" className="btn primary loginBtn">Back to sign in</Link></>:<><div><div className="eyebrow">Reset password</div><h2>Choose a new password</h2><p className="muted">Use at least 8 characters.</p></div>{error?<div className="notice butter">{error}</div>:null}<form className="authForm" onSubmit={submit}><label className="muted small">New password</label><input className="input" type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} required/><label className="muted small">Confirm password</label><input className="input" type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/><button className="btn primary loginBtn" disabled={busy||!token}>{busy?'Updating…':'Set new password'}</button></form>{!token?<p className="muted small">This reset link is missing its token. Request a new link.</p>:null}</>}</div></section></main>}
