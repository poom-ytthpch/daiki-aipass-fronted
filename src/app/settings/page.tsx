'use client';
import {useEffect,useState} from 'react';
type Prefs={defaultModel:string;responseStyle:string};const DEFAULT:Prefs={defaultModel:'auto',responseStyle:'balanced'};
export default function Settings(){const [prefs,setPrefs]=useState(DEFAULT);const [saved,setSaved]=useState(false);useEffect(()=>{const t=window.setTimeout(()=>{try{setPrefs({...DEFAULT,...JSON.parse(localStorage.getItem('daiki_preferences')||'{}')})}catch{}},0);return()=>window.clearTimeout(t)},[]);const save=()=>{localStorage.setItem('daiki_preferences',JSON.stringify(prefs));setSaved(true);setTimeout(()=>setSaved(false),1600)};return <><header className="pageHeader"><div><div className="eyebrow">Preferences</div><h1>Settings</h1><p>Choose how your own Daiki workspace starts. These preferences stay in this browser.</p></div></header><section className="settingsSurface surface"><div className="settingRow"><div><strong>Default route</strong><p>Which model alias Chat selects when a new session opens.</p></div><select className="input" value={prefs.defaultModel} onChange={e=>setPrefs(x=>({...x,defaultModel:e.target.value}))}><option value="auto">Auto</option><option value="fast">Fast</option><option value="balanced">Balanced</option><option value="deep">Deep</option></select></div><div className="settingRow"><div><strong>Response style</strong><p>Your preferred answer style for future personalization.</p></div><select className="input" value={prefs.responseStyle} onChange={e=>setPrefs(x=>({...x,responseStyle:e.target.value}))}><option value="concise">Concise</option><option value="balanced">Balanced</option><option value="detailed">Detailed</option></select></div><div className="settingsFooter"><button className="btn primary" onClick={save}>{saved?'Saved ✓':'Save preferences'}</button></div></section></>}
