import {backendFetch} from '@/lib/backend';
type Model={id?:string;owned_by?:string};
export default async function Models(){let models:Model[]=[];try{const r=await backendFetch('/v1/models');if(r.ok){const d=await r.json() as {data?:Model[]};models=d.data||[]}}catch{}return <><div className="top"><div><div className="h1">Models & Routes</div><div className="muted">Stable aliases exposed by Daiki; physical runtimes stay private and can change independently.</div></div></div><section className="card"><table className="table"><thead><tr><th>Alias</th><th>Routing</th><th>Purpose</th></tr></thead><tbody>{models.length?models.map((m,i)=><tr key={m.id||i}><td><b>{m.id||'unknown'}</b></td><td>Daiki Router → controlled queue → LiteLLM</td><td>{m.id==='fast'?'Light Q&A, rewrite, extraction':m.id==='deep'?'Coding, debugging, multi-step reasoning':m.id==='balanced'?'General mixed workloads':'Capability + complexity based'}</td></tr>):<tr><td><b>auto</b></td><td>Router waiting</td><td>Physical model remains private</td></tr>}</tbody></table></section></>}
