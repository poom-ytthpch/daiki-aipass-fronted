import Link from 'next/link';
import {ArrowRight,LockKeyhole} from 'lucide-react';
export const dynamic='force-dynamic';
type LoginProps={searchParams:Promise<{error?:string}>};
export default async function Login({searchParams}:LoginProps){const {error}=await searchParams;const googleEnabled=Boolean(process.env.GOOGLE_CLIENT_ID);return <main className="loginPage"><section className="loginLayout"><div className="loginStory"><Link className="loginBrand" href="/"><span>Daiki<span>●</span></span><small>AI Passport</small></Link><div className="storyCopy"><div className="eyebrow">A little AI companion</div><h1>Small questions.<br/>Big possibilities.</h1><p>Chat, think, plan and learn with a private AI workspace designed to feel simple from the first hello.</p><div className="storyTags"><span className="tag peach">Private</span><span className="tag sky">Friendly</span><span className="tag mint">Controlled</span></div></div><div className="storyFooter"><LockKeyhole size={16}/> Your model routes and credentials stay behind Daiki.</div></div><div className="loginPanel"><div><div className="eyebrow">Welcome</div><h2>Come say hello to Daiki</h2><p className="muted">Sign in with your account, or create one in a few seconds.</p></div>{error==='google_not_configured'?<div className="notice butter">Google sign-in is not configured for this environment yet.</div>:null}<div className="authActions"><a href="/api/auth/login" className="btn primary loginBtn">Sign in with email <ArrowRight size={16}/></a><a href="/api/auth/register" className="btn ghost loginBtn">Create account</a></div><Link className="forgotLink" href="/forgot-password">Forgot password?</Link><div className="authDivider"><span>or</span></div>{googleEnabled?<a href="/api/auth/login?provider=google" className="btn googleBtn"><span className="googleMark">G</span>Continue with Google</a>:<button type="button" className="btn googleBtn" disabled><span className="googleMark">G</span>Continue with Google <span className="oauthState">Not configured</span></button>}<p className="loginFine">New accounts begin with a small Chat allowance while an administrator reviews access.</p></div></section></main>}
